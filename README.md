# IV-PPO Setup

## Experiment Checklist

    - [ ] Environment activated (`conda activate iv_rl`)
    - [ ] Dependencies installed (`pip install -r requirements_py9.txt`)
    - [ ] WandB API key configured in `.env`
    - [ ] Scripts prepared for parallel experiments
    - [ ] Tmux sessions configured
    - [ ] Logs monitored for progress
